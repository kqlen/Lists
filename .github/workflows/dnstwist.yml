name: dnstwist daily
on:
  workflow_dispatch
  schedule:
    - cron: "0 5 * * *"

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Show tree
        run: |
          pwd
          ls -la

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dnstwist
        run: |
          python -m pip install --upgrade pip
          pip install dnstwist

      - name: Run get_dnstwist.py
        run: |
          python get_dnstwist.py domains_list.txt || python3 get_dnstwist.py domains_list.txt
          ls -1 *.csv || true

      - name: Stage CSV to data
        run: |
          mkdir -p data
          latest=$(ls -1t *.csv | head -n 1)
          if [ -z "$latest" ]; then
            echo "No CSV produced. Check previous step logs."
            exit 1
          fi
          cp "$latest" data/dnstwist_latest.csv

      - name: Commit CSV if changed
        run: |
          git config user.name "github-actions"
          git config user.email "actions@users.noreply.github.com"
          git add data/dnstwist_latest.csv
          git diff --cached --quiet || git commit -m "Update dnstwist CSV"
          git push
